{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Debug DataCluster",
            "request": "launch",
            "mainClass": "Main",     // or just "Main" if no package
            "classPaths": [
                "${workspaceFolder}/out" // ensure this points to absolute path
            ],
            "args": "data/iris_bezdek.txt phase4"
        }
    ]
}